export const HOST = "http://localhost:3001";

const AUTH_ROUTE = `${HOST}/api/auth`;
const TOKEN_ROUTE = `${HOST}/getToken`;

export const GET_CALL_TOKEN = `${HOST}/generate-token`;
export const CHECK_USER_ROUTE = `${AUTH_ROUTE}/check-user`;
